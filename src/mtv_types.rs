#[derive(Debug)]
pub struct Movie {
    pub name: String,
    pub year: String,
    pub poster_addr: String,
    pub size: String,
    pub exists: bool,
    pub path: String,
    pub index: String,
    pub movid: String,
}
